interface:
  display_name: "批量处理图片"
  short_description: "调用 ImageMagick 全部命令行能力，完成批量转换、GIF、尺寸及高级图片处理"
  default_prompt: "使用 $batch-image-processing 处理我指定的图片，检查本机能力，保留原图并验证输出。"
policy:
  allow_implicit_invocation: true
